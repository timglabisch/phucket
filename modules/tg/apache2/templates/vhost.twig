<VirtualHost {{ listen }}>

    {% for k, attribute in attributes %}
        {{ k }} {{ attribute }}
    {% endfor %}

    {% for directory in directories %}
        <Directory {{ directory.path }}>

        {% for k, attribute in directory.attributes %}
            {{ k }} {{ attribute }}
        {% endfor %}

        </Directory>
    {% endfor %}

</VirtualHost>